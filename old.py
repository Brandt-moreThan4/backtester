


    # # Show the cumulative returns in a bar chart
    # total_rets_fig = px.bar(total_rets)
    # total_rets_fig.update_yaxes(tickformat=".2%",title_text="Total Return")
    # st.plotly_chart(total_rets_fig) 